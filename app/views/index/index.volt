{{ content() }}

<h1>CPDN Identity Provider</h1>
<h3>IdP is part of CPDN platform. It manages identities of users and integrates OAuth 2 server.</h3>
<p style="font-size:20px">It is written in <span class="label label-default">Phalcon PHP (2.0.9)</span>. As an OAuth 2 server is used Brent Shaffer's <span class="label label-default">OAuth2 Server PHP</span>.</p>